# OVOS spec stack under test — the combo selector.
#
# Each line below is a *pin* for one repo in the integrated spec stack. This
# file (NOT a `pip install .[test]`) is what gets installed in CI, so pip's
# resolver is handed an explicit, fully-pinned set of git refs and never gets
# the freedom to downgrade or re-resolve a transitive dep out from under the
# combination we are trying to validate.
#
# To validate a DIFFERENT combination of unmerged branches: edit the refs here
# and open a PR — CI then installs exactly that stack and runs the full spec
# conformance suite against it. As each branch merges, flip its ref to @dev.
#
# Limitation: two branches of the SAME repo cannot both be installed; pick one
# ref per repo.

# core with STOP-1 conformance source (PR #777). The old combined
# test/spec-stack-integration-proof branch is stale: it still caps
# ovos-spec-tools<1.0.0, which cannot co-resolve with the spec-tools 1.x stack
# (ovos-adapt-parser 1.5.0a1 requires ovos-spec-tools>=1.4.0a1). feat/stop-1-
# conformance is rebased on current dev (spec-tools>=1.1.0a1,<2.0.0), carries
# the INTENT-4 §10 orchestrator manifest, and is the live STOP-1 PR branch.
git+https://github.com/OpenVoiceOS/ovos-core@feat/stop-1-conformance
# INTENT-4 producer (emits ovos.intent.register.* etc.)
git+https://github.com/OpenVoiceOS/ovos-workshop@feat/intent-4-producer
# session spec fields (active_handlers / converse_handlers / response_mode)
ovos-bus-client>=2.4.0a1
# pipeline engines (workshop-9-compatible)
# adapt @dev carries the None-blacklist guard (#47): the INTENT-3 e2e registers a
# keyword intent for a session whose blacklisted_intents/blacklisted_skills are
# None, and adapt's match filter does `intent_type not in sess.blacklisted_intents`
# — a TypeError on None that the matcher swallows, so the positive match silently
# never fires while the negative cases pass. @dev guards it with `(… or [])`.
git+https://github.com/OpenVoiceOS/ovos-adapt-pipeline-plugin@dev
# padatious @dev (1.8.0a1) now carries the §6 INTENT-4 template spec-topic
# consumer AND the merged OVOS-CONTEXT-1 requires/excludes_context gating (#80),
# and allows ovos-spec-tools>=1.4.0a1 — so it co-resolves with the 1.x stack.
# The stale feat/intent-4-adoption branch capped spec-tools<1.0.0. It is NOT
# load-bearing for the orchestrator suites (those drive PADACIOSO_HIGH).
git+https://github.com/OpenVoiceOS/ovos-padatious-pipeline-plugin@dev
# Per-plugin INTENT-4 registration-compliance matchers (test_intent4_plugins_
# conformance.py). Each is pinned at the ref whose spec-test status its registry
# entry documents:
#   nebulento / palavreado / markov — INTENT-4 adoption merged to @dev -> spec
#                            test GREEN (§6 template / §5 keyword consumers live
#                            on @dev).
#   ovos-m2v-pipeline      — INTENT-4 adoption merged to @dev -> spec test GREEN
#                            (runs in prototype mode against the smallest model2vec model).
#   linha-fina             — @dev DOES carry the §6 consumer, but a real plugin
#                            bug (lru_cache keyed on the unhashable Session, the
#                            same defect padacioso fixed) raises TypeError under
#                            bus-client 2.x and blocks ALL matching, so BOTH its
#                            per-plugin tests xfail until the cache-key fix lands.
git+https://github.com/OpenVoiceOS/nebulento@dev
git+https://github.com/OpenVoiceOS/palavreado@dev
git+https://github.com/OpenVoiceOS/ovos-m2v-pipeline@dev
git+https://github.com/OpenVoiceOS/linha-fina@dev
git+https://github.com/OpenVoiceOS/ovos-markov-pipeline-plugin@dev
# fixture skills (ovos-workshop cap lifted to <10)
git+https://github.com/OpenVoiceOS/ovos-skill-parrot@dev
git+https://github.com/OpenVoiceOS/ovos-skill-fallback-unknown@dev
git+https://github.com/OpenVoiceOS/ovos-skill-count@dev
git+https://github.com/OpenVoiceOS/ovos-skill-hello-world@dev
# COMMON-QUERY-1 pipeline plugin under test (ovos.common_query.* contest).
# Loaded as pipeline_id "ovos-common-query-pipeline-plugin"; the CQ skill-side
# driver is injected by the suite as an in-process CommonQuerySkill fixture, so
# no extra skill package is pinned for it.
git+https://github.com/OpenVoiceOS/ovos-common-query-pipeline-plugin@dev
# PERSONA-1 pipeline plugin under test (persona match/dispatch + ovos.persona.*).
# Loaded as pipeline_ids "ovos-persona-pipeline-plugin-high"/"-low". A throwaway
# echo persona is injected by the suite via pipeline_config (personas_path), so
# no solver backend or network is required.
#   @dev now lifts the ovos-workshop cap to <10.0.0 (the workshop-9 compat fix
#   merged), so it resolves against the workshop>=9 integration branch.
git+https://github.com/OpenVoiceOS/ovos-persona@dev
# e2e harness lib (None-iter-guard fix landed on dev)
git+https://github.com/OpenVoiceOS/ovoscope@dev
# GUI service (NamespaceManager) — drives the GUI-1 service-contract clauses
# (SYSTEM_ prefix gating, per-session namespace stack, reserved-key stripping).
# The producer-side GUI-1 clauses need only ovos-bus-client's GUIInterface; if
# this install is unavailable the GUI service classes skip cleanly.
git+https://github.com/OpenVoiceOS/ovos-gui@dev
# published spec vocabulary
ovos-spec-tools>=0.17.1a1
# padacioso matcher: drops the unhashable-Session lru_cache key so it matches
# under ovos-bus-client 2.x (PyPI 1.0.0 raises TypeError on every match).
# TODO flip to @dev once OpenVoiceOS/padacioso#... (the lru_cache fix) merges.
git+https://github.com/OpenVoiceOS/padacioso@dev
# audio input service under test (AUDIO-IN-1 conformance) — emits the spec
# ovos.listener.* / ovos.utterance.handle topics natively on @dev.
git+https://github.com/OpenVoiceOS/ovos-dinkum-listener@dev
# audio output service under test (AUDIO-1 conformance) — PlaybackService.
# @dev emits ovos.audio.output.* / ovos.mic.listen on the spec namespace but
# still subscribes legacy names for b64/queue/play_sound/is_speaking/audio.stop
# (those clauses xfail until the rename lands).
git+https://github.com/OpenVoiceOS/ovos-audio@dev
# pipeline engine fallback + test deps
pytest
pytest-json-report

# ovos-media — the OVOS Virtual Media Player (OVOS-OCP-1 §2/§3/§4). Drives the
# OCPPlayerHarness in test_ocp1_conformance.py. It is NOT listed here because
# ovos-media pins `ovos-bus-client~=1.3` (even on @dev), which conflicts with the
# bus-client>=2.4.0a1 the rest of this stack requires; a flat resolve would
# downgrade bus-client and break every other suite. It runs fine against
# bus-client 2.x at runtime, so the integration workflow installs it with
# --no-deps as a separate step (see .github/workflows/integration.yml). The
# OCP-1 suite skips cleanly when ovos-media is absent.
