# OVOS spec stack under test — the combo selector.
#
# Each line below is a *pin* for one repo in the integrated spec stack. This
# file (NOT a `pip install .[test]`) is what gets installed in CI, so pip's
# resolver is handed an explicit, fully-pinned set of git refs and never gets
# the freedom to downgrade or re-resolve a transitive dep out from under the
# combination we are trying to validate.
#
# To validate a DIFFERENT combination of unmerged branches: edit the refs here
# and open a PR — CI then installs exactly that stack and runs the full spec
# conformance suite against it. As each branch merges, flip its ref to @dev.
#
# Limitation: two branches of the SAME repo cannot both be installed; pick one
# ref per repo.

# core with STOP-1 + PIPELINE-1 conformance source (combined integration branch)
git+https://github.com/OpenVoiceOS/ovos-core@test/spec-stack-integration-proof
# INTENT-4 producer (emits ovos.intent.register.* etc.)
git+https://github.com/OpenVoiceOS/ovos-workshop@feat/intent-4-producer
# session spec fields (active_handlers / converse_handlers / response_mode)
ovos-bus-client>=2.4.0a1
# pipeline engines (workshop-9-compatible)
# adapt @dev carries the None-blacklist guard (#47): the INTENT-3 e2e registers a
# keyword intent for a session whose blacklisted_intents/blacklisted_skills are
# None, and adapt's match filter does `intent_type not in sess.blacklisted_intents`
# — a TypeError on None that the matcher swallows, so the positive match silently
# never fires while the negative cases pass. @dev guards it with `(… or [])`.
git+https://github.com/OpenVoiceOS/ovos-adapt-pipeline-plugin@dev
git+https://github.com/OpenVoiceOS/ovos-padatious-pipeline-plugin@dev
# fixture skills (ovos-workshop cap lifted to <10)
git+https://github.com/OpenVoiceOS/ovos-skill-parrot@dev
git+https://github.com/OpenVoiceOS/ovos-skill-fallback-unknown@dev
git+https://github.com/OpenVoiceOS/ovos-skill-count@dev
git+https://github.com/OpenVoiceOS/ovos-skill-hello-world@dev
# e2e harness lib (None-iter-guard fix landed on dev)
git+https://github.com/OpenVoiceOS/ovoscope@dev
# published spec vocabulary
ovos-spec-tools>=0.17.1a1
# padacioso matcher: drops the unhashable-Session lru_cache key so it matches
# under ovos-bus-client 2.x (PyPI 1.0.0 raises TypeError on every match).
# TODO flip to @dev once OpenVoiceOS/padacioso#... (the lru_cache fix) merges.
git+https://github.com/OpenVoiceOS/padacioso@dev
# pipeline engine fallback + test deps
pytest
pytest-json-report
