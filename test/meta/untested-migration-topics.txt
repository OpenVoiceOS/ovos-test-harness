# MIGRATION_MAP legacy topics whose twin no back-compat cell drives.
#
# A ratchet: an entry may be removed once a cell under test/backcompat/ names
# both the legacy and the spec topic, never added. The four intent-management
# renames here carry lossy payload transforms, which is exactly where an
# undriven twin hands a legacy consumer a payload it cannot read.
complete_intent_failure
detach_intent
detach_skill
mycroft.audio.play_sound
mycroft.audio.queue
mycroft.audio.speak.status
mycroft.audio.speech.stop
mycroft.awoken
mycroft.mic.listen
mycroft.skill.disable_intent
mycroft.skill.enable_intent
mycroft.stop
recognizer_loop:record_begin
recognizer_loop:record_end
recognizer_loop:sleep
skill.stop.pong
speak:b64_audio
speak:b64_audio.response

# Pending back-compat coverage — owner: harness lane. Real fix is driven cells
# under test/backcompat/, not this list. These four are the FALLBACK-1 wire
# twins (backcompat owns that surface). No back-compat cell drives them yet;
# ovos-test-harness#64's ping test is under test/conformance and does not count.
# They exist in MIGRATION_MAP from ovos-spec-tools 1.12.0a1 (the floor in
# requirements.txt). Each self-flips to a hard failure here the day a cell
# names both spellings, which forces its removal.
ovos.skills.fallback.register
ovos.skills.fallback.deregister
ovos.skills.fallback.ping
ovos.skills.fallback.pong
